server {
    host = "0.0.0.0"
    grpcPort = 9090
    httpPort = 8080
}

database {
    host = "localhost"
    port = 5432
    database = "azathoth"
    user = "azathoth"
    password = "azathoth_dev"
    maxPoolSize = 20
}

redis {
    host = "localhost"
    port = 6379
    password = "azathoth_dev"
}

kafka {
    bootstrapServers = "localhost:9092"
    groupId = "player-service"
}

player {
    cache {
        maxSize = 10000
        expireAfterWriteSeconds = 600
    }
    session {
        timeoutSeconds = 1800
    }
    inventory {
        maxSlots = 64
    }
}
