server {
    host = "0.0.0.0"
    grpcPort = 9090
    httpPort = 8080
}

database {
    host = "localhost"
    port = 5432
    database = "azathoth"
    user = "azathoth"
    password = "azathoth_dev"
    maxPoolSize = 10
}

redis {
    host = "localhost"
    port = 6379
    password = "azathoth_dev"
}

kafka {
    bootstrapServers = "localhost:9092"
    groupId = "trade-service"
}

trade {
    auctionHouse {
        maxListings = 50
        listingDurationHours = 48
        taxRate = 0.05
    }
    directTrade {
        timeoutSeconds = 120
        maxItemsPerSide = 12
    }
}
